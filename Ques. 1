/* Sum of the elements in the array */

#include <iostream>

using namespace std;

int main()
{
   int arr[]={1,2,23,45,56};
   int n = sizeof(arr)/sizeof(arr[0]);
   long long int sum = 0;
   for(int i=0;i<n;i++)
   {
       sum +=arr[i];
   }
   
   cout<<sum<<endl;
   
    return 0;
}
